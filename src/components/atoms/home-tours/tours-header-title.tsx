import { font_Syne_800 } from "@/styles/global-styles";
import styled from "styled-components";

const ToursHeaderTitle = styled.h2`
  ${font_Syne_800};
  font-size: var(--primary-font-size-base);
  text-transform: uppercase;
`;

export default ToursHeaderTitle;
